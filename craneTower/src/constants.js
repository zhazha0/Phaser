
const WIDTH_SCENE = window.innerWidth * 1.5 // the larger, the easier
const HEIGHT_SCENE = window.innerHeight * 1.5
const HOOK_NEAR_MIN = 150
const HOOK_FAR_MAX = 330
const HOOK_UP_MIN = 0.5
const HOOK_DOWN_MAX = 10.0

export default {
    WIDTH_SCENE,
    HEIGHT_SCENE,
    HOOK_NEAR_MIN,
    HOOK_FAR_MAX,
    HOOK_UP_MIN,
    HOOK_DOWN_MAX
}